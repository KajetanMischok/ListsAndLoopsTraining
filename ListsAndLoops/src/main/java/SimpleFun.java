public class SimpleFun {
    /**
     * Converts Minutes into Seconds
     * @param minutes
     * @return seconds
     */
    public static int getSeconds(int minutes){
        return 0;
    }

    /**
     * Accepts a measurement value in inches and returns the equivalent of the measurement value in feet
     * Notes:
     *     If inches are under 12, return 0.
     *     12 inches = 1 foot.
     *
     * @param inches
     * @return value in feet
     */
    public static int inchesToFeet(int inches){
        return 0;
    }

    /**
     * Checks if String has the same number of x's and o's and returns either true or false.
     *
     * Rules:
     *     Return a boolean value (true or false).
     *     Return true if the amount of x's and o's are the same.
     *     Return false if they aren't the same amount.
     *     The string can contain any character.
     *     When "x" and "o" are not in the string, return true.
     *     Must be case insensitive.
     * @param str
     * @return
     */
    public static boolean equalNumberOfXsAndOs(String str) {
        return true;
    }
}
